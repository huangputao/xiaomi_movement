server:
  #端口号
  port: 9999

spring:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
  mvc:
    hiddenmethod:
      filter:
        enabled: true

  datasource:
    #1.JDBC
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/xiaomi_movement?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&useSSL=false
    username: root
    password: 123456

#cron表达式
schedules:
  run: 0 10 16 * * ?

